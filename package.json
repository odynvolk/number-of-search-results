{
  "name": "number-of-search-results",
  "version": "1.0.3",
  "license": "MIT",
  "description": "Small module for retrieving number of search results for a given query from different search engines.",
  "contributors": [
    "Alexi Rahman",
    "David Vicente"
  ],
  "dependencies": {
    "cheerio": "^0.22.0",
    "request": "^2.83.0",
    "request-promise": "^4.2.2",
    "sanitize-html": "^1.16.1"
  },
  "devDependencies": {
    "assert": "^1.4.1",
    "chai": "^4.1.2",
    "chai-as-promised": "^7.1.1",
    "coffee-script": "^1.12.7",
    "eslint": "^4.12.1",
    "mocha": "^4.0.1",
    "mocha-cakes-2": "^2.1.0",
    "nock": "^9.1.4"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/odynvolk/number-of-search-results.git"
  },
  "scripts": {
    "start": "node app.js",
    "test": "mocha && eslint . --cache",
    "revision": "which git && [[ -d .git ]] && git rev-parse HEAD > config/_revision || true",
    "ci": "npm install && npm prune && npm ls && npm test"
  },
  "files": [
    "lib",
    "index.js"
  ],
  "main": "index.js"
}
